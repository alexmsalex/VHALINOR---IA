# Opção 1: Configuração padrão
from trading_config import TradingConfig

config = TradingConfig()
SYMBOL = config.symbol
INTERVAL = config.interval
QUANTIDADE = config.quantity
RISK_PER_TRADE = config.risk_per_trade

# Opção 2: Perfil específico
from trading_config import PerfilTrading

config = PerfilTrading.moderado()
# ou
config = PerfilTrading.scalping()
# ou
config = PerfilTrading.conservador()

# Opção 3: Configuração personalizada
config = TradingConfig(
    symbol="ETHUSDT",
    interval="5m",
    quantity=0.01,
    risk_per_trade=0.02,
    stop_loss_percent=3.0,
    take_profit_percent=6.0
)